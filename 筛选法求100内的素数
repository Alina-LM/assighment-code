# assighment-code
#include<stdio.h>
int main()
{
	int prime[101] = { 0 };
	prime[0] = prime[1] = 1;
	for (int i = 2;i * i <= 100;i++)
	{
		if(!prime[i])
		for (int j = i * i;j <= 100;j += i)
			prime[j] = 1;
	}
	for (int i = 2, n = 1;i <= 100;i++)
	{
		if (!prime[i])
		{
			printf(" %d", i);
			if (n++ % 5 == 0)printf("\n");
		}
	}
}
